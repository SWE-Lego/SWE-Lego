[core]
workspace_base="SWE-Lego/OpenHands-0.53.0"

[llm.eval_qwen3_8b]
model="openai/Qwen/Qwen3-8B"
api_key="dummy-key"
base_url="http://localhost:8000/v1"
temperature=0.0
max_input_tokens=147456
max_output_tokens=16384

[llm.eval_qwen3_32b]
model="openai/Qwen/Qwen3-32B"
api_key="dummy-key"
base_url="http://localhost:8000/v1"
temperature=0.0
max_input_tokens=147456
max_output_tokens=16384

# LLM-based summarizing condenser for evaluation
[condenser.summarizer_for_eval]
type = "llm"
llm_config = "openai/Qwen/Qwen3-8B-Condenser"  # Reference to an LLM config to use for summarization
keep_first = 2        # Number of initial events to always keep
max_size = 25        # Maximum size of history before triggering summarization

# Recent events condenser for evaluation
[condenser.recent_for_eval_first2_max10]
type = "recent"
keep_first = 2        # Number of initial events to always keep
max_events = 10       # Maximum number of events to keep in history
